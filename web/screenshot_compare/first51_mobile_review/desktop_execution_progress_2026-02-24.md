# 前51项电脑端执行进展

## 一 执行方式
本轮按执行文档在电脑端循环执行以下命令。

- `npm run -s screenshot:web:visible52`
- `npm run -s screenshot:compare:visible52`

执行日期 2026-02-24。

## 二 已落地改动
- 新增前51项真实手机对比脚本 `web/scripts/compareFirst51MobileScreenshots.mjs`，已生成逐项面板图和总表。
- 修复学习队列保底逻辑 `web/src/state/selectors.ts`，在规划词为空或当日额度耗尽时回退可学习词，减少暂无单词。
- 调整电脑端截图基线 `web/scripts/qaVisible52.mjs`，加入 `androidVisual=1` 视觉模式并预置生词本数据。
- 修复44号点前置状态 `web/scripts/qaVisible52.mjs`，截图前强制切到生词本词书后再打开清空弹窗。
- 优化查词详情底部区 `web/src/App.tsx` 与 `web/src/index.css`，增加与安卓接近的已在生词本主按钮样式。
- 调整提前复习弹窗为底部抽屉形态 `web/src/App.tsx`，并同步脚本检查点。
- 新增安卓视觉模式状态条占位 `web/src/App.tsx` 与 `web/src/index.css`，减少顶部布局错位。

## 三 当前对比结果
数据来源 `web/screenshot_compare/report/compare_visible52_summary.json`，统计范围前51项。

- 平均差异率 14.8342%
- 高差异 6 项
- 中差异 16 项
- 低差异 29 项

高差异编号。

- 13 复习时间弹窗可打开 77.0017%
- 31 查词详情可打开 39.1421%
- 32 详情翻译按钮可见 39.1406%
- 33 详情 AI 助记按钮可见 39.1406%
- 34 详情生词本按钮可切换 40.4049%
- 44 清空生词本确认弹窗可打开 85.6697%

## 四 视觉结论
- 目前前51项已全部可稳定截图，功能动作链路保持通过。
- 但视觉仍未达到完美一致，主要剩余在两类页面。
- 第一类是全屏遮罩弹窗页，13和44受遮罩色、弹窗尺寸和文本排版影响最大。
- 第二类是查词详情页，31到34在抽屉高度、文本密度和按钮布局上与安卓仍有明显偏差。

## 五 下一轮精修焦点
- 13和44做专用弹窗样式，不复用通用对话框模板。
- 31到34做专用查词详情抽屉模板，对齐安卓底部主按钮和信息分区。
- 完成后继续执行电脑端前51项截图与对比，直至高差异清零。

TLDR：前51项电脑端流程已经稳定并完成多轮修复，当前仍有6项高差异，集中在复习时间弹窗、查词详情抽屉和清空生词本弹窗，尚未达到完美一致。
