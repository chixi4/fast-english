**人工视觉审阅报告 2026-02-23**
本报告为模型逐图人工审阅，不使用像素差异结果作为结论依据。

审阅对象目录
- 安卓截图 `web/screenshot_compare/android_visible52`
- 网页截图 `web/screenshot_compare/web_visible52`
- 配对面板 `web/screenshot_compare/report/panels_visible52`

总体结论
- 人工判定一致 0 项
- 人工判定不一致 52 项
- 其中功能页错位 4 项
- 其余 48 项均存在不同程度的布局、文本层级、控件尺寸或状态同步偏差

逐项结论
| 编号 | 功能点 | 人工结论 | 差异类型 | 主要问题 |
|---|---|---|---|---|
| 01 | 底部标签 学习 可见 | 不一致 | 基础布局与视觉密度偏差 | 页面框架相同，但状态栏、字号、卡片尺寸、留白和底部栏高度与安卓不一致 |
| 02 | 底部标签 查词 可见 | 不一致 | 基础布局与视觉密度偏差 | 页面框架相同，但状态栏、字号、卡片尺寸、留白和底部栏高度与安卓不一致 |
| 03 | 底部标签 词库 可见 | 不一致 | 基础布局与视觉密度偏差 | 页面框架相同，但状态栏、字号、卡片尺寸、留白和底部栏高度与安卓不一致 |
| 04 | 底部标签 我的 可见 | 不一致 | 基础布局与视觉密度偏差 | 页面框架相同，但状态栏、字号、卡片尺寸、留白和底部栏高度与安卓不一致 |
| 05 | 学习页已加载 | 不一致 | 基础布局与视觉密度偏差 | 页面框架相同，但状态栏、字号、卡片尺寸、留白和底部栏高度与安卓不一致 |
| 06 | 认词模式按钮可见 | 不一致 | 基础布局与视觉密度偏差 | 页面框架相同，但状态栏、字号、卡片尺寸、留白和底部栏高度与安卓不一致 |
| 07 | 拼写模式按钮可见 | 不一致 | 基础布局与视觉密度偏差 | 页面框架相同，但状态栏、字号、卡片尺寸、留白和底部栏高度与安卓不一致 |
| 08 | 当前词书信息可见 | 不一致 | 基础布局与视觉密度偏差 | 页面框架相同，但状态栏、字号、卡片尺寸、留白和底部栏高度与安卓不一致 |
| 09 | 学习进度文本可解析 | 不一致 | 基础布局与视觉密度偏差 | 页面框架相同，但状态栏、字号、卡片尺寸、留白和底部栏高度与安卓不一致 |
| 10 | 认词卡正面可见 | 不一致 | 基础布局与视觉密度偏差 | 页面框架相同，但状态栏、字号、卡片尺寸、留白和底部栏高度与安卓不一致 |
| 11 | 点击翻卡可看到背面 | 不一致 | 同功能不同状态与层级 | 安卓弹窗是大号底部弹层，网页是窄小卡片弹层，层级、尺寸、按钮位置都不一致 |
| 12 | 翻回正面成功 | 不一致 | 基础布局与视觉密度偏差 | 页面框架相同，但状态栏、字号、卡片尺寸、留白和底部栏高度与安卓不一致 |
| 13 | 复习时间弹窗可打开 | 不一致 | 同功能不同状态与层级 | 安卓显示完整释义页，网页仍停在简化卡片态，内容层级和文本量明显不足 |
| 14 | 复习时间弹窗可关闭 | 不一致 | 基础布局与视觉密度偏差 | 页面框架相同，但状态栏、字号、卡片尺寸、留白和底部栏高度与安卓不一致 |
| 15 | 认词三按钮区域可见 | 不一致 | 基础布局与视觉密度偏差 | 页面框架相同，但状态栏、字号、卡片尺寸、留白和底部栏高度与安卓不一致 |
| 16 | 左滑太简单快捷按钮可触发 | 不一致 | 同功能不同状态与层级 | 同一功能已出现，但当前词条状态、文本层级、控件尺寸与安卓不同 |
| 17 | 太简单撤销可执行 | 不一致 | 同功能不同状态与层级 | 同一功能已出现，但当前词条状态、文本层级、控件尺寸与安卓不同 |
| 18 | 右滑加入生词本快捷按钮可触发 | 不一致 | 同功能不同状态与层级 | 同一功能已出现，但当前词条状态、文本层级、控件尺寸与安卓不同 |
| 19 | 切换到拼写模式成功 | 不一致 | 同功能不同状态与层级 | 都进入拼写模式，但网页控件密度和间距明显更紧，顶部信息条与安卓不一致 |
| 20 | 拼写面板可见 | 不一致 | 同功能不同状态与层级 | 同一功能已出现，但当前词条状态、文本层级、控件尺寸与安卓不同 |
| 21 | 首字母提示可展开 | 不一致 | 同功能不同状态与层级 | 安卓首字母提示显示在辅助区，网页同区域文案与状态计数不同步 |
| 22 | 长度提示可展开 | 不一致 | 同功能不同状态与层级 | 安卓长度提示区和错误次数区关系清晰，网页布局压缩导致信息分区不清晰 |
| 23 | 拼写错误提示可见 | 不一致 | 同功能不同状态与层级 | 同一功能已出现，但当前词条状态、文本层级、控件尺寸与安卓不同 |
| 24 | 三次错误进入抄写阶段 | 不一致 | 同功能不同状态与层级 | 安卓进入抄写阶段后文本区与按钮区层级明确，网页同区块边界和间距偏差明显 |
| 25 | 抄写正确后可继续 | 不一致 | 同功能不同状态与层级 | 同一功能已出现，但当前词条状态、文本层级、控件尺寸与安卓不同 |
| 26 | 拼写 AI 助记按钮可见 | 不一致 | 同功能不同状态与层级 | 同一功能已出现，但当前词条状态、文本层级、控件尺寸与安卓不同 |
| 27 | 切回认词模式成功 | 不一致 | 同功能不同状态与层级 | 同一功能已出现，但当前词条状态、文本层级、控件尺寸与安卓不同 |
| 28 | 进入查词页成功 | 不一致 | 基础布局与视觉密度偏差 | 页面框架相同，但状态栏、字号、卡片尺寸、留白和底部栏高度与安卓不一致 |
| 29 | 查词输入框可见 | 不一致 | 基础布局与视觉密度偏差 | 页面框架相同，但状态栏、字号、卡片尺寸、留白和底部栏高度与安卓不一致 |
| 30 | 查词结果可出现 | 不一致 | 基础布局与视觉密度偏差 | 页面框架相同，但状态栏、字号、卡片尺寸、留白和底部栏高度与安卓不一致 |
| 31 | 查词详情可打开 | 不一致 | 同功能不同状态与层级 | 查词详情弹层安卓为宽内容面板，网页为窄卡片，释义和例句展示明显减少 |
| 32 | 详情翻译按钮可见 | 不一致 | 同功能不同状态与层级 | 同一功能已出现，但当前词条状态、文本层级、控件尺寸与安卓不同 |
| 33 | 详情 AI 助记按钮可见 | 不一致 | 同功能不同状态与层级 | 同一功能已出现，但当前词条状态、文本层级、控件尺寸与安卓不同 |
| 34 | 详情生词本按钮可切换 | 不一致 | 同功能不同状态与层级 | 生词本按钮交互存在，但网页按钮文案和状态反馈位置与安卓不一致 |
| 35 | 关闭查词详情成功 | 不一致 | 同功能不同状态与层级 | 同一功能已出现，但当前词条状态、文本层级、控件尺寸与安卓不同 |
| 36 | 进入词库页成功 | 不一致 | 基础布局与视觉密度偏差 | 页面框架相同，但状态栏、字号、卡片尺寸、留白和底部栏高度与安卓不一致 |
| 37 | 词库卡片列表可见 | 不一致 | 基础布局与视觉密度偏差 | 页面框架相同，但状态栏、字号、卡片尺寸、留白和底部栏高度与安卓不一致 |
| 38 | 词书详情弹窗可打开 | 不一致 | 同功能不同状态与层级 | 词书详情弹窗安卓含完整统计和环形图，网页版信息密度较低，关键统计缺项 |
| 39 | 词书详情弹窗可关闭 | 不一致 | 基础布局与视觉密度偏差 | 页面框架相同，但状态栏、字号、卡片尺寸、留白和底部栏高度与安卓不一致 |
| 40 | 提前复习弹窗可打开 | 不一致 | 同功能不同状态与层级 | 同一功能已出现，但当前词条状态、文本层级、控件尺寸与安卓不同 |
| 41 | 提前复习全选按钮可点击 | 不一致 | 同功能不同状态与层级 | 同一功能已出现，但当前词条状态、文本层级、控件尺寸与安卓不同 |
| 42 | 提前复习清空按钮可点击 | 不一致 | 同功能不同状态与层级 | 同一功能已出现，但当前词条状态、文本层级、控件尺寸与安卓不同 |
| 43 | 提前复习确认后关闭 | 不一致 | 基础布局与视觉密度偏差 | 页面框架相同，但状态栏、字号、卡片尺寸、留白和底部栏高度与安卓不一致 |
| 44 | 清空生词本确认弹窗可打开 | 不一致 | 同功能不同状态与层级 | 清空生词本确认窗安卓居中大弹窗，网页是右上小弹窗，交互重心位置完全不同 |
| 45 | 清空生词本确认弹窗可取消 | 不一致 | 基础布局与视觉密度偏差 | 页面框架相同，但状态栏、字号、卡片尺寸、留白和底部栏高度与安卓不一致 |
| 46 | 词书教程页可打开 | 不一致 | 功能页错位 | 功能页错位。安卓是词书构建教程页，网页截图停在词库主页 |
| 47 | 词书教程页可返回 | 不一致 | 基础布局与视觉密度偏差 | 页面框架相同，但状态栏、字号、卡片尺寸、留白和底部栏高度与安卓不一致 |
| 48 | 我的页热力图可见 | 不一致 | 基础布局与视觉密度偏差 | 安卓我的页展示大图表与热力图，网页只显示压缩统计卡，信息层级缺失 |
| 49 | 学习数据页切换功能可用 | 不一致 | 功能页错位 | 功能页错位。安卓是学习数据页，网页仍停在学习统计页 |
| 50 | 数据恢复确认弹窗可打开并取消 | 不一致 | 基础布局与视觉密度偏差 | 页面框架相同，但状态栏、字号、卡片尺寸、留白和底部栏高度与安卓不一致 |
| 51 | 今日新词页可进入并确认 | 不一致 | 功能页错位 | 功能页错位。安卓是今日新词选择页，网页停在词库页 |
| 52 | 实验室成本与隐私弹窗可打开 | 不一致 | 功能页错位 | 功能页错位。安卓是实验室配置页，网页停在学习统计页 |

## 补充复核 2026-02-23 第二轮
本节基于同日重新生成的网页截图目录 `web/screenshot_compare/web_visible52`，对第一轮报告中的四个功能页错位项做人工复核补充。

复核范围
- `visible_46`
- `visible_49`
- `visible_51`
- `visible_52`

补充结论
- 功能页错位项已从 4 项降为 0 项。
- `visible_46` 网页与安卓均为词书构建教程页，页面状态一致。
- `visible_49` 网页与安卓均为学习数据页，页面状态一致。
- `visible_51` 网页与安卓均为今日新词选择页，页面状态一致。
- `visible_52` 网页与安卓均为实验室页，页面状态一致。
- 上述四项虽然页面归位成功，但仍存在字号、留白、控件尺寸等视觉偏差，整体仍归入视觉不一致待收敛项。

## 补充复核 2026-02-23 第三轮
本节基于当日新一轮网页截图结果进行人工视觉审阅。审阅方式为逐对查看安卓图与网页图，不使用像素差异值做结论。

执行记录
- 网页截图命令 `npm run -s screenshot:web:visible52`
- 对比命令 `npm run -s screenshot:compare:visible52`
- 自动结果仍为 52 通过 0 失败，视觉对比结果仍为一致 0 差异 52

本轮重点审阅编号
- `visible_01`
- `visible_31`
- `visible_38`
- `visible_44`
- `visible_48`
- `visible_49`
- `visible_51`
- `visible_52`

人工审阅结论
1. `visible_01` 学习页首词与进度保持对齐，网页词面字号已上调，但主词字体仍小于安卓，卡片纵向留白仍偏多。
2. `visible_31` 查词详情已固定为 `abandonment`，并改为底部抽屉形态，页面状态已对齐；剩余差异集中在正文字号、按钮高度和底栏视觉重量。
3. `visible_38` 词书详情已固定为 `完整词库`，并改为底部抽屉形态，目标对象与入口链路一致；剩余差异集中在统计块密度、环形图尺寸和文案排版。
4. `visible_44` 清空生词本弹窗已改为居中确认窗，结构已接近安卓；剩余差异是弹窗宽高比例、按钮圆角和按钮文字垂直位置。
5. `visible_48` 我的页标题已回到 `我的`，页面层级更接近安卓；剩余差异是统计卡内部字重、折线图线宽与热力图单元尺寸。
6. `visible_49` 已回到历史模式，且手势统计与今日认词都为 0，页面状态与安卓一致；剩余差异是学习日历卡片内边距、折线图坐标字体与分区间距。
7. `visible_51` 今日新词页输入框文案已改为 `搜索词条`，页面对象一致；剩余差异是按钮尺寸、列表行高、词条释义换行密度。
8. `visible_52` 实验室页结构链路稳定，开关和配置区均可见；剩余差异是区块间距、控件高度、标题字重和底部栏高度。

本轮总评
- 页面对象一致性继续提升，关键对象偏差已从目标错误逐步收敛为样式细节偏差。
- 视觉仍未达到完美复刻标准，当前瓶颈已经明确转为全局字体系统、控件尺寸系统和图表绘制风格的统一收口。

## 补充复核 2026-02-23 第四轮
本轮在第三轮基础上只做两项全局可读性修正，分别是正文字号提升到 16px，以及认词按钮最小触控高度提升到 44px，并重新执行全量截图与人工审阅。

执行结果
- 执行命令 `npm run -s screenshot:web:visible52`，结果 52 通过 0 失败。
- 执行命令 `npm run -s screenshot:compare:visible52`，结果一致 0 差异 52。
- 人工复核 `visible_01` 与 `visible_49`，确认可读性提升且功能页无回归。

补充结论
- 学习页主区域文本与按钮可读性继续提升，但与安卓仍存在字号层级和间距差异。
- 学习数据页结构仍保持同页同态，当前差异仍集中在图表风格和布局密度。

## 补充复核 2026-02-23 第五轮
本轮继续做视觉收敛，目标是进一步压缩学习页主词尺寸差异、学习数据页曲线区信息密度差异和确认弹窗宽度差异。

执行内容
- 学习页主词字号再次上调。
- 学习数据页记忆曲线改为无图例展示，并在长周期时隐藏横轴文字，降低信息拥挤。
- 清空确认弹窗继续加宽，使宽高比例更接近安卓确认窗。
- 底栏图标、统计区样式、热力图尺寸和触控尺寸保持上一轮结果。

执行结果
- 执行命令 `npm run -s screenshot:web:visible52`，结果 52 通过 0 失败。
- 执行命令 `npm run -s screenshot:compare:visible52`，结果一致 0 差异 52。
- 人工复核 `visible_01`、`visible_44`、`visible_48`、`visible_49`。

补充结论
1. `visible_01` 主词尺寸继续接近安卓，但网页字重和字距仍略轻，卡片上半区留白仍偏大。
2. `visible_49` 记忆曲线区域杂讯明显减少，阅读密度更接近安卓，剩余差异主要在标题字重与区块上下间距。
3. `visible_44` 确认窗宽度更接近安卓，按钮区比例改善，剩余差异是标题字重和按钮文本垂直对齐。
4. `visible_48` 我的页统计区结构稳定，仍有图表线宽和热力图区块密度差异。
